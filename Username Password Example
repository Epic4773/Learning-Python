import random
import string

name = input('Enter your FULL name (First Middle Last):  ')
first, middle, last = name.split()

username = first[0].lower()+middle[0].lower()+last.lower()
print('Your username is '+username)
email_tag = '@student.district.org'
email = username + email_tag
print('Your email address is '+email)
word  = input('Enter a word you will remember, that is also at least 4 characters long:  ')
num = random.choice(string.digits)
char = random.choice(string.punctuation)
password = first[0].upper() + last[0] + word.lower() + num + char
print('Your password is '+ password)

answer = input('would you like to change your password? (y/n) ')
if answer == 'y' :
  new_password = input('please enter your new password:  ')
  if len(new_password) < 8 :
    print('your password must be at least 8 characters long')
  else :
    password = new_password
    print('you password has been changed to '+password)
if answer == 'n' :
  print('your password will not be changed')
